# Plan

Build paper 68 `ebm_transformer_compositional_grasping` from the shared pool, compile PDF to Downloads only, and publish the exact-name public repo.

2026-06-15 continuation plan:

1. Re-audit the real MuJoCo grasping evidence before changing the terminal decision.
2. Verify code compilation, training/main/ablation/stress CSV integrity, learned and analytic baselines, uncertainty, PDF rebuild, Downloads-only artifact placement, and public GitHub availability.
3. Keep `KILL_ARCHIVE` unless the EBM/Transformer composition beats force closure, CEM, MLP energy, Transformer ranking, ensemble uncertainty, and monolithic ablations under held-out composition shift.
4. Preserve the paper as a negative result because the current evidence shows useful internal ablation signal but fails the external baseline gate.
